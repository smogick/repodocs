# archive/

Сюда переносятся целиком (папкой) полностью завершённые или неактуальные проекты/области из `projects/` и `areas/` — не удаляются, а перекладываются сюда как есть, с сохранением структуры и `status: archived` во frontmatter.
